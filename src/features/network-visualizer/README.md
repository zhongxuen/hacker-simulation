# src/features/network-visualizer

The fog-of-war network map, driven by engine state, plus a text/table view for accessibility. Arrives in phase 07.

Public API: `index.ts` only. Never import here: another feature's internals.
