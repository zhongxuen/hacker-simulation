# src/features/terminal

The browser terminal UI and command registry, running on the simulation engine, including beginner mode (guided first run, friendly errors). Arrives in phase 05.

Public API: `index.ts` only. Never import here: another feature's internals.
