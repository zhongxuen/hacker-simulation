# src/lib

Cross-cutting helpers: the Supabase client, local storage for guest progress, and small utilities.

Never import here: features, `src/app`, or React components. The simulation engine must not import from here.
