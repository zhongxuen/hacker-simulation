# src/app/(app)

The product surface: campaign, missions, terminal, learning center, profile. Playable in guest mode, so no route here may require sign-up to start.

Never import here: a feature's internals (use `@/features/<name>`) or simulation internals. Keep pages thin.
