# src/features/achievements

XP, levels, rank titles, achievements, and cosmetic rewards. No dark patterns: earned progress is never lost. Arrives in phase 08.

Public API: `index.ts` only. Never import here: another feature's internals.
