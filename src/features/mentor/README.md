# src/features/mentor

The AI Mentor character UI and the hint ladder. It gives hints, never answers, and hints never cost XP. Arrives in phase 10.

Public API: `index.ts` only. Never import here: another feature's internals, or any API key (model calls go through `src/app/api`).
