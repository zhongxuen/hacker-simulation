# src/components

Shared, feature-agnostic React components: `ui/` (design-system primitives), `shell/` (the app shell around every product page), and `settings/` (the controls on `/settings`).

Never import here: features, `src/app`, or simulation internals. Components receive data through props.
