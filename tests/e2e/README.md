# tests/e2e

Playwright end-to-end tests: one happy path per module. The config stub is `playwright.config.ts`; tests arrive in phase 11.

Never import here: application internals. Drive the app through the browser the way a learner would.
