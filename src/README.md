# src

Application source. Folders are separated by concern (routes, simulation, content, features, UI), and the boundaries between them are enforced by ESLint (`eslint.config.mjs`), not only by convention.
