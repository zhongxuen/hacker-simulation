# src/sim/\_\_fixtures\_\_/golden

Committed output of the golden run: the transcript and the final state. Written by `src/sim/golden.test.ts` when missing, and compared byte for byte after that. Never edit these by hand; Prettier skips this folder so formatting can't change them.

Never import here: nothing. These are data files.
